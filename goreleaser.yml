build:
  binary_name: planrockr-cli
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64
brew:
  # Reporitory to push the tap to.
  repo: planrockr/planrockr-cli

  # Folder inside the repository to put the formula.
  # Default is the root folder.
  folder: Formula
